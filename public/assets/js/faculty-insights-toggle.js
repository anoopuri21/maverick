/**
 * Maverick Business Academy
 * Faculty Insights Toggle — REMOVED for CLS safety
 * Previously handled Read more / Read less clamp.
 * Now full content is always visible, no toggle, no layout shift.
 * File kept to avoid 404 but does nothing.
 * Core Web Vitals: CLS safe, no JS-driven height changes.
 */
(function () {
  "use strict";
  // No-op: full content visible, no clamp, no toggle
  // If you need to re-enable toggle in future, ensure it doesn't cause CLS
  // by reserving space or using max-height transitions.
})();
